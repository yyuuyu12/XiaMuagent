================================================================================
产品需求明细（根据近期对话整理）
文档说明：记录你提出的需求点，以及截至整理时在「当前仓库/线上」是否应视为已生效。
生成日期：2026-04-11
================================================================================

一、你提出的需求清单（按出现顺序）
--------------------------------------------------------------------------------

【1】语音识别（ASR）配置提示
  现象：提示「语音识别服务未配置，请联系管理员配置 ASR 服务地址」。
  期望：管理员可通过配置使用 ASR；技术上除数据库 system_config.asr_url 外，支持环境变量
        ASR_URL 作为后备（Zeabur 等部署场景）。

【2】历史记录展示不完整
  期望：历史记录在展开后能看到完整文案（不仅钩子/标题），复制时能带上正文；
        「历史」页与灵感页内历史列表行为一致。

【3】「我的」页布局调整
  期望：退出登录按钮尽量靠页面下方，会员/升级入口区域略增高、更醒目（留白与高度优化）。

【4】登录与注册交互（第二批）
  期望：进入应用不要一上来全屏登录打断；仅在需要消耗服务（生成、提取、改写等）时再提示登录；
        登录/注册用底部浮层（Bottom Sheet），不要用整页登录。
  附带：顶部「登录」入口、未登录访客在「我的」的引导、相关接口调用前的登录校验与 401 处理等。

【5】工程协作约定（非功能需求）
  期望：每次改完代码后，由助手完成：提交（commit）并推送到 GitHub（push），流程保持简单，
        不要擅自做复杂合并/恢复操作而不先对齐你的意图。

【6】版本策略说明（你后续澄清）
  期望：以「最新版本」为准，不要从历史提交强行「恢复」旧文件覆盖当前主线。


二、实现与生效情况（对照当前仓库主线状态说明）
--------------------------------------------------------------------------------

说明：合并远端代码时，前端 H5 曾以远端版本为主解决冲突，导致部分仅存在于本机提交中的
      前端改动未保留在最终合并后的 content-creator-app.html / backend/public/index.html。
      以下「是否生效」按该事实归纳；若你本地 Cursor 未保存缓冲与磁盘不一致，以 git 仓库为准。

【1】ASR 环境变量后备
  状态：后端 backend/routes/douyinToText.js 中已支持 ASR_URL 与库内 asr_url 组合使用（曾并入合并提交）。
  是否生效：代码在仓库中应已存在；需在 Zeabur/服务器配置 ASR_URL 或数据库 asr_url 后，线上才不再报未配置。

【2】历史记录展开显示全文 + 复制含正文
  状态：曾在 content-creator-app.html 中实现（history-script-content、scriptCopyText 等），
        合并后以远端 HTML 为主后，上述前端实现大概率未保留在当前仓库的 H5 源文件中。
  是否生效：当前仓库 H5 侧——视为「未生效或需重新合入」；需再次改前端并同步 index.html 后部署。

【3】我的页：退出下移 + 会员块增高
  状态：同上，曾为 profile-tab-page、profile-logout、vip-upgrade-card 等样式/结构改动；
        合并后远端「我的」页若已改版（如编辑资料、头像裁剪），与你当时要的布局可能不一致。
  是否生效：当前仓库 H5 侧——视为「未生效或需在与新版我的页合并后重做」。

【4】延后登录 + 底部浮层登录/注册
  状态：完整方案包含：去掉默认全屏登录页、showAuthSheet、tAuthSheet、ensureLoggedIn、
        fetchMe/init 行为、顶栏登录、访客态等；合并后当前磁盘上的 H5 仍为全屏 tLogin + page 切换逻辑。
  是否生效：当前仓库 H5 侧——视为「未生效」，需基于现有远端「我的/资料编辑/头像」再集成一版。

【5】改完即 commit + push
  状态：已执行过一次合并提交并 push 到 origin/master；过程中曾因远端领先产生合并，与你的「只推我的改动」
        预期有摩擦。
  是否生效：流程上「能推上去」已达成；后续应按你要求固定为：小步提交 + 推送，冲突时先问你策略。

【6】不要强行恢复旧提交覆盖最新
  状态：已按你意思不再做「从 0949396 整文件恢复」类操作。
  是否生效：约定层面已对齐。


三、建议的后续动作（产品/研发）
--------------------------------------------------------------------------------
1. 在「当前 GitHub 最新 H5（含资料编辑、头像裁剪等）」基础上，重新合入：
   - 延后登录 + 底部登录浮层
   - 历史全文展开与复制
   - 我的页布局（与新版 UI 统一视觉后）
2. 部署侧：配置 ASR_URL 或后台 asr_url，验证抖音转文案链路。
3. 每次发版：content-creator-app.html 改完后执行同步规则
   cp content-creator-app.html backend/public/index.html
   再 git add / commit / push。

================================================================================
（完）
